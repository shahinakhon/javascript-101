// Good Morning 🌞
// Codédex

let hour = 4;

if (hour < 12) {
    console.log( "Good morning🌞, it is", hour, "o'clock",);
} else if (hour > 12 && hour < 18) {
    console.log("Good afternoon, it is", hour, "o'clock");
} else if (hour > 18 && hour < 24) {
    console.log("Good evening🌆, it is", hour, "o'clock");
}
